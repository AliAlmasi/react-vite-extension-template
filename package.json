{
  "name": "react-vite-extension-template",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "ts": "tsc -b",
    "build:chrome": "npm run ts && vite build --mode chrome",
    "build:firefox": "npm run ts && vite build --mode firefox",
    "build": "npm run build:chrome && npm run build:firefox"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.6",
    "@types/react": "^19.1.12",
    "@types/react-dom": "^19.1.9",
    "@vitejs/plugin-react": "^5.0.2",
    "typescript": "^5.9.2",
    "vite": "^6.3.6",
    "vite-plugin-static-copy": "^3.1.2"
  }
}
